# サーバー起動手順

## 概要

このシステムは2つのサーバーで構成されています：
- **Webアプリ（ポート3000）**: Next.js + Express + Socket.IO
- **AI-Board（ポート5000）**: Flask + Socket.IO

## 起動方法

### 方法1: 統合スクリプトで両方起動（推奨）

プロジェクトのルートディレクトリ（`02_Projects`）で実行：

```powershell
.\start_all_servers.ps1
```

これで両方のサーバーが別ウィンドウで起動します。

### 方法2: 個別に起動

#### Webアプリ（ポート3000）の起動

```powershell
cd 02_1_App_postit_board\src
.\start_server.ps1
```

または

```powershell
cd 02_1_App_postit_board\src
npm run dev
```

#### AI-Board（ポート5000）の起動

```powershell
cd 02_2_AI-Board
.\start_server.ps1
```


または（仮想環境を使用する場合）

```powershell
cd 02_2_AI-Board
.venv\Scripts\Activate.ps1
python src\webapp\app.py
```

または（仮想環境がない場合）

```powershell
cd 02_2_AI-Board
python src\webapp\app.py
```

## アクセス

起動後、以下のURLでアクセスできます：

- **Webアプリ**: http://localhost:3000
- **AI-Board**: http://localhost:5000
- **ボードページ例**: http://localhost:3000/board/odgeqgf

## トラブルシューティング

### ポートが既に使用されている場合

#### ポート3000を確認
```powershell
netstat -ano | findstr :3000
```

#### ポート5000を確認
```powershell
netstat -ano | findstr :5000
```

使用中のプロセスを終了する場合：
```powershell
taskkill /PID <プロセスID> /F
```

### 依存関係がインストールされていない場合

#### Webアプリ
```powershell
cd 02_1_App_postit_board\src
npm install
```

#### AI-Board
```powershell
cd 02_2_AI-Board
pip install -r requirements.txt
```

### サーバーが起動しない場合

1. **Node.jsがインストールされているか確認**
   ```powershell
   node --version
   npm --version
   ```

2. **Pythonがインストールされているか確認**
   ```powershell
   python --version
   ```

3. **環境変数が設定されているか確認**
   - `02_2_AI-Board\.env` ファイルに `GEMINI_API_KEY` が設定されているか確認

4. **ログを確認**
   - 各サーバーのコンソール出力を確認してエラーメッセージを確認

## 注意事項

- 両方のサーバーを起動する必要があります
- カメラ検知機能を使用する場合は、`sticky_note_detector.py` も別途起動してください
- ボードIDは `sticky_note_detector.py` の `BOARD_ID` 変数で設定されています（デフォルト: `"odgeqgf"`）
